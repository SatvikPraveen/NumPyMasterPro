# Development, test, and tooling dependencies.
# Runtime dependencies live in requirements.txt; install both for a full dev setup:
#   pip install -r requirements.txt -r requirements_dev.txt
# Or, equivalently, use the extras defined in pyproject.toml:
#   pip install -e ".[dev,app,notebooks]"
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
pytest-timeout>=2.3.0
hypothesis>=6.100.0

# Linting, formatting, typing
ruff>=0.6.0
mypy>=1.10.0
pre-commit>=3.7.0
bandit>=1.7.0

# Notebook execution (CI validation)
nbconvert>=7.16.0
nbformat>=5.10.0
ipykernel>=6.29.0
